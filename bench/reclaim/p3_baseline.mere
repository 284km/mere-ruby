// The same string work with nothing stored: the floor to compare against.
let rec go = fn (acc: int) -> fn (i: int) ->
  if i >= 1000000 then acc
  else go (acc + str_len ("value number " ++ str_of_int i)) (i + 1);
print ("total: " ++ str_of_int (go 0 0) ++ "\n")
